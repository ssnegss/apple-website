export type AnimationProps = {
    transform?: string,
    opacity?: number,
    scale?: number,
    ease?: string,
    y?: number,
    duration?: number,
}

export type ScrollProps = {
    scrub?: number,
}