import { ModelView as ModelViewType } from "../types/ModelView";

const ModelView = ({
   index,
   groupRef,
   gsapType,
   controlRef,
   setRotationState,
   item,
   size,
}: ModelViewType) => {
   return <div>model</div>;
};

export default ModelView;
